stages:          # List of stages for jobs, and their order of execution
  - build
#  - test
  - deploy

build-job:       # This job runs in the build stage, which runs first.
  stage: build
  before_script:
    - cp ${ENV} .env
  script:
    - echo "Starting build-job..."
#    - docker build -t booking-api-image .
    - docker compose build

#unit-test-job:   # This job runs in the test stage.
#  stage: test    # It only starts when the job in the build stage completes successfully.
#  script:
#    - echo "Running unit tests..."
#
#lint-test-job:   # This job also runs in the test stage.
#  stage: test    # It can run at the same time as unit-test-job (in parallel).
#  script:
#    - echo "Linting code..."
#    - sleep 5

deploy-job:      # This job runs in the deploy stage.
  stage: deploy  # It only runs when *both* jobs in the test stage complete successfully.
  script:
    - echo "Deploying application..."
#    - docker compose --file docker-compose-ci.yml restart booking_back booking_celery_worker booking_celery_beat
#    - docker compose restart booking_back booking_celery_worker booking_celery_beat
    - docker compose up
    - echo "Application successfully deployed."
